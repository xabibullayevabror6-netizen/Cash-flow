[pytest]
testpaths = tests
python_files = test_*.py
# `app` paketi backend/ ildizida — pytest uni yo'lda ko'rishi uchun
pythonpath = .
addopts = -q --tb=short
filterwarnings =
    ignore::UserWarning
